import { h } from 'preact';
import './middlesection.scss';

const HeaderLayout = props => (
    <div class="middle-section">
        <h1 class="heading"> Sample debug Application </h1>
        <p class="text">Debugging is the process of finding and resolving defects or problems within a system.</p>
    </div>
);

export default HeaderLayout;
